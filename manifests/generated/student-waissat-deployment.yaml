---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: student-waissat
  namespace: container-course-week01
  labels:
    app: student-app
    student: waissat
    week: "1"
spec:
  replicas: 1
  selector:
    matchLabels:
      app: student-app
      student: waissat
  template:
    metadata:
      labels:
        app: student-app
        student: waissat
    spec:
      containers:
      - name: student-app
        image: ghcr.io/waissat/container-course-student:3-student
        ports:
        - containerPort: 5000
          name: http
        env:
        - name: STUDENT_NAME
          value: "Wais Satari"
        - name: GITHUB_USERNAME
          value: "waissat"
        resources:
          requests:
            memory: "64Mi"
            cpu: "50m"
          limits:
            memory: "256Mi"
            cpu: "200m"
        livenessProbe:
          httpGet:
            path: /health
            port: 5000
          initialDelaySeconds: 10
          periodSeconds: 30
        readinessProbe:
          httpGet:
            path: /health
            port: 5000
          initialDelaySeconds: 5
          periodSeconds: 10
---
apiVersion: v1
kind: Service
metadata:
  name: student-waissat-svc
  namespace: container-course-week01
  labels:
    student: waissat
spec:
  selector:
    app: student-app
    student: waissat
  ports:
  - port: 80
    targetPort: 5000
    protocol: TCP
    name: http
