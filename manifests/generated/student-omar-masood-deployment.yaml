---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: student-omar-masood
  namespace: container-course-week01
  labels:
    app: student-app
    student: omar-masood
    week: "1"
spec:
  replicas: 1
  selector:
    matchLabels:
      app: student-app
      student: omar-masood
  template:
    metadata:
      labels:
        app: student-app
        student: omar-masood
    spec:
      containers:
      - name: student-app
        image: ghcr.io/omar-masood/container-course-student:v3-student
        ports:
        - containerPort: 5000
          name: http
        env:
        - name: STUDENT_NAME
          value: "Omar Masood"
        - name: GITHUB_USERNAME
          value: "omar-masood"
        resources:
          requests:
            memory: "64Mi"
            cpu: "50m"
          limits:
            memory: "256Mi"
            cpu: "200m"
        livenessProbe:
          httpGet:
            path: /health
            port: 5000
          initialDelaySeconds: 10
          periodSeconds: 30
        readinessProbe:
          httpGet:
            path: /health
            port: 5000
          initialDelaySeconds: 5
          periodSeconds: 10
---
apiVersion: v1
kind: Service
metadata:
  name: student-omar-masood-svc
  namespace: container-course-week01
  labels:
    student: omar-masood
spec:
  selector:
    app: student-app
    student: omar-masood
  ports:
  - port: 80
    targetPort: 5000
    protocol: TCP
    name: http
